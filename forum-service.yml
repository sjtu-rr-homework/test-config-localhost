
server:
  port: 8096

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8061/eureka/
      
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/moment
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 11MB
